<body><main><h1>Editing Book</h1>
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @book, url: book_path(@book.id), method: :patch do |f| %>
  <br
  <P>Title</P>
  <%= f.text_field :title %>

  <p>Body</p>
  <%= f.text_area :body %>

  <p><%= f.submit 'Update Book' %></p>
  
  
  <table><td><%= link_to "Show", book_path(@book.id) %></td><td>|</td><td><%= link_to "Back", books_path(@book.id) %></td></table>

<% end %>
</main></body